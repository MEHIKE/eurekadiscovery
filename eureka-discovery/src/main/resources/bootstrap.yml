server:
  port: 8061

spring:
  application:
    name: eureka-discovery
  cloud:
    config:
      enabled: true
      #uri: http://config:8088
      uri: http://${CONFIG_SERVICE_HOST}:${CONFIG_SERVICE_PORT}
      